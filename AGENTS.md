Hoje o produto vive em **quatro repositórios** agrupados na pasta local `mvp-propostas/` (contexto único para a IA). Fonte de verdade do produto: **`docs/status-projeto.md` neste repo da API**.

```
mvp-propostas/
├── api/      ← este repositório (Spring Boot + docs)
├── app/      ← Angular orçamentos (:4200)
├── lp/       ← landing page (:4300)
└── admin/    ← painel interno (:4400)
```

## Para agentes

Antes de informar status ou iniciar implementação, ler **`docs/status-projeto.md`**. Ao concluir entregas relevantes, atualizar esse arquivo na mesma sessão ou PR.
